Buffer debugging 20260131

Ring buffer holds 1536 stereo samples
#define DAC_BUFFER_UNI 1536*2 

Stereos samples pr 250µs packets
11 to 48

One packet is 1/32 to 1/139 full buffer.


Is the response much too slow? 1 correction in 2000*11 to 2000*48 samples
#define SI_PKG_RESOLUTION	2000 // 1000			// Apply 1/2 IIR filter. Resolution: once every 2000 packets at 250µs packet rate


Max' device is
2024071703BSB

and is likely this:
	
commit 4d164b3e02bd5b66d27b976aad46db9fd2454d30
Author: Borge Strand-Bergesen <borge.strand@gmail.com>
Date:   Sat Jul 20 19:06:00 2024 +0200
	
Head as of 20260202
3caaf2e302c12f10aac8b460ce5a9fbc48753266	

Look for 
Usb_unicode('7')



# -DINDICATE_EXTREME_SI			Indication of extreme skip/insert events
							#ifdef INDICATE_EXTREME_SI
								mobo_led(FLED_GREEN);
								mobo_led(FLED_RED);
								mobo_led_select(FREQ_NOCHANGE, input_select);
							#endif

SPK_GAP_USKIP green
SPK_GAP_LSKIP



To do list:

Calculate the maximum clock deviation that the system can correct

Measure the maximum clock deviation 
- Use frequency counter to characterize oscillators, generator
- Use USB mux and cables on spare board to switch between generator channels
- Force the correction system into failure

Add hard brakes for writing within +-2 max packet lengths from reading head






Reusing ADC input I2S port toward SPDIF receiver.

Clock configuration
- Needed whenever the frequency rate changes
- Identical division setting for 44.1 and 48
- Clock setup is done with pm_gc_setup(), move all occurances to a function..
  In UAC2 it is called repeatedly in uac2_usb_specific_request.c
  In UAC1 it is called once in AK5394A_task_init of taskAK5349.c




Power control in HW_GEN_DIN_20
- Quite messy with patches on one board but not the other
- IO pins accessed in taskAK5394.c
- IO pins accessed in composite_widget.c



Silencer code
- Which chunks of code try to clear the output buffers?
- Which chunks of code will mute the outgoing I2S interface?



Other notes
- AK5394A_task_init() doesn't really start a task. Is called from hpsdr_AK5394A_task_init(), uac1_AK5394A_task_init(), ..... and presumably uac2.... which does. 